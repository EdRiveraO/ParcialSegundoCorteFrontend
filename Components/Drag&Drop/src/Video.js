// eslint-disable-next-line no-unused-vars
class Elements {
  constructor(title, subtitle, sources, description, thumb) {
    this.title = title;
    this.subtitle = subtitle;
    this.sources = sources;
    this.description = description;
    this.thumb = thumb;
  }
}
